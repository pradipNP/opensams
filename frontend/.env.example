# LOCAL DEVELOPMENT only — do not use these values in a production build
VITE_API_URL=http://localhost:5001/api/v1

# PRODUCTION (set this at frontend build time; Vite inlines the value)
# VITE_API_URL=https://your-api-origin/api/v1
